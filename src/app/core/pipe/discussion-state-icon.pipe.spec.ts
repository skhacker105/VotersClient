import { DiscussionStateIconPipe } from './discussion-state-icon.pipe';

describe('DiscussionStateIconPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscussionStateIconPipe();
    expect(pipe).toBeTruthy();
  });
});
