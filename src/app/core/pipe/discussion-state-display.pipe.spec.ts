import { DiscussionStateDisplayPipe } from './discussion-state-display.pipe';

describe('DiscussionStateDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscussionStateDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
