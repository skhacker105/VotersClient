import { DiscussionStateClassPipe } from './discussion-state-class.pipe';

describe('DiscussionStateClassPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscussionStateClassPipe();
    expect(pipe).toBeTruthy();
  });
});
